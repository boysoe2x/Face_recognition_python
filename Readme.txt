* Để có thể chạy đc chương trình cần có đầy đủ các thư viện python:
- Chú thích: cài thư viện thì lên gg search quả pip install <tên thư viện> nhá :))
1) Chắc chắn phải cài python và pip rồi :))
2) Cài tkinter -> thư viện vẽ giao diện cho winform bằng python
3) Cài opencv -> thư viện xử lý ảnh cơ bản bao gồm cả cv2
4) Cài face_recognition -> thư viện core để nhận diện khuôn mặt:
4.1) Cài Cmake -> cần có để có thể cài và biên dịch dlib.
4.2) Cài dlib -> "một bộ công cụ C ++ hiện đại chứa các thuật toán học máy và các 
công cụ để tạo phần mềm phức tạp trong C ++ để giải quyết các vấn đề trong thế giới thực."
Theo gg thì nó nôm la như vậy :)) mà thư viện này sẽ cần để có thể cài face_recognition.
- Một vài lỗi khi cài dlib như ko tìm đc thư viện PIL-chính là pillow mà nó ngáo nên ko nhận
-> gỡ và cái lại pillow là xong.
- Cần có visual studio cài sẵn tiện ích C++ compiler (Ảnh C++compiler.png ấy) éo có nó ko chạy đâu :))
4.3) Cài face_recognition -> core đấy ko cài có cái nịt nó chạy nhá :))